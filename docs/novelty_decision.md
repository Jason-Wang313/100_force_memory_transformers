# Novelty Decision

Decision: conditionally promising.

The v4.1 continuation evidence supports keeping the idea alive as STRONG_REVISE. The mechanism is more specific than generic transformer memory or generic uncertainty filtering: it targets persistent hidden force state in contact-rich manipulation.

The novelty is not yet enough for ICLR main because trained-model evidence, external benchmarks, and manual prior-work depth are missing.
